#include <stdio.h>

int main() {
    int mat[2][2] = {{4, 5}, {2, 3}};
    int determinant;
    determinant = (mat[0][0] * mat[1][1]) - (mat[0][1] * mat[1][0]);

    printf("The determinant of the matrix is: %d\n", determinant);

    return 0;
}
